# Import all models to ensure they are registered with SQLAlchemy
# This module should be imported before any database operations

# Import all models to ensure they are registered with SQLAlchemy
# This module should be imported before any database operations

# Import all models from the shared models file
from app.shared.model.models import *
